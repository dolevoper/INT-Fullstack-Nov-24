
const Firstvariable = Number(prompt("Enter first variable to calculate:"));
while (Number.isInteger(Firstvariable))
     else { alert("Error! Please enter a valid number");
       const Firstvariable = Number(prompt("Enter first variable to calculate:"));
      }
     
const changingOperator = prompt("Choose an operator: +, -, *, or /");
while (changingOperator === "+" || changingOperator === "-" || changingOperator === "*" || changingOperator === "/")
   else {
      alert("Error! Please a valid operator");
      const changingOperator = prompt("Choose an operator: +, -, *, or /");
   }      
const Secondvariable = Number(prompt("Enter second variable to calculate:"));
   while (Secondvariable === isNaN)
        {
          alert("Error! Please enter a valid number");
          const Secondvariable = Number(prompt("Enter second variable to calculate:"));  
        }
