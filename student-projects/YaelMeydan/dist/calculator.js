var Firstvariable = Number(prompt("Enter first variable to calculate:"));
while (Number.isInteger(Firstvariable))
    ;
{
    alert("Error! Please enter a valid number");
    var Firstvariable_1 = Number(prompt("Enter first variable to calculate:"));
}
var changingOperator = prompt("Choose an operator: +, -, *, or /");
while (changingOperator === "+" || changingOperator === "-" || changingOperator === "*" || changingOperator === "/")
    ;
{
    alert("Error! Please a valid operator");
    var changingOperator_1 = prompt("Choose an operator: +, -, *, or /");
}
var Secondvariable = Number(prompt("Enter second variable to calculate:"));
while (Secondvariable === isNaN) {
    alert("Error! Please enter a valid number");
    var Secondvariable_1 = Number(prompt("Enter second variable to calculate:"));
}
